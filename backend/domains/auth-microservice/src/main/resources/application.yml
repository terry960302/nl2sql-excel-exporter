server:
  port: 8081

spring:
  profiles:
    active: local

  application:
    name: auth-service

# 타임아웃 등 Feign 공통 설정
feign:
  client:
    config:
      default:
        connectTimeout: 5000   # 연결 타임아웃 (ms)
        readTimeout: 10000     # 읽기 타임아웃 (ms)

